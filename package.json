{
  "name": "nest-react",
  "description": "Nest React Application inside a Turbo mono repository",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  },
  "devDependencies": {
    "turbo": "^1.12.2"
  }
}
