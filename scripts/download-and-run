#!/bin/sh
cd ~
if ! [ -d ./payroll-smb ]; then
    echo "The ./payroll-smb folder does not exist. Let's create it."
    mkdir payroll-smb
fi
cd payroll-smb
curl -O -s -S https://raw.githubusercontent.com/serhii-untilov/Payroll-SMB/master/docker-compose.yml
curl -O -s -S https://raw.githubusercontent.com/serhii-untilov/Payroll-SMB/master/start
curl -O -s -S https://raw.githubusercontent.com/serhii-untilov/Payroll-SMB/master/stop
curl -O -s -S https://raw.githubusercontent.com/serhii-untilov/Payroll-SMB/master/update
curl -O -s -S https://raw.githubusercontent.com/serhii-untilov/Payroll-SMB/master/logs
curl -O -s -S https://raw.githubusercontent.com/serhii-untilov/Payroll-SMB/master/stats
if ! [ -f ./.env.docker ]; then
    curl -O -s -S https://raw.githubusercontent.com/serhii-untilov/Payroll-SMB/master/.env.docker
fi
chmod +x ./start
chmod +x ./stop
chmod +x ./update
chmod +x ./logs
chmod +x ./stats
./stop
docker compose pull
./start
